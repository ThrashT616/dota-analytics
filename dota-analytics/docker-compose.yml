services:
  postgres:
    image: postgres:18
    container_name: dota_analytics_postgres
    environment:
      POSTGRES_DB: dota_analytics
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: